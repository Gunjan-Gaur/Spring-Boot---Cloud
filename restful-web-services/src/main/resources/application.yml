server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  security:
    user:
      name: gunjan
      password: gunjan
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    defer-datasource-initialization: true
    show-sql: true
  h2:
    console:
      enabled: true